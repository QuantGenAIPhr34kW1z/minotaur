# Results folder

This directory contains generated artifacts from `make run`, `make sweep`, and `make plots`.

- CSV outputs are deterministic given a config.
- Figures are generated by gnuplot scripts in `/plots`.

Do not commit large sweep outputs by default; keep the repository audit-friendly.
